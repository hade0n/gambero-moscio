/**
 * Punto di accesso all'hook della collezione locali.
 * L'implementazione vive nel Provider (una sola istanza per l'intera app).
 */
export { useRestaurants } from '../context/RestaurantsContext.jsx';
